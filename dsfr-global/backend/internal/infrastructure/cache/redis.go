// Package cache is retained as a placeholder. The former Redis-backed token
// store was replaced by a Postgres-backed store (see internal/infrastructure/
// persistence/postgres/token_store.go), removing the Redis dependency.
package cache
